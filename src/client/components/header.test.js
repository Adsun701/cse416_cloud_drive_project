//import Header from './header';

//test('check to make sure header contains string `Cloud Drive Manager`.', () => {
//    const testHeader = header();

//    expect(testHeader).toMatch(/Cloud Drive Manager/);
//});
