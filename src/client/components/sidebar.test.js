//import { Sidebar } from './sidebar';

//test('check to make sure header contains string `Add Snapshot`.', () => {
//    const testSidebar = Sidebar();
//    expect(testSidebar).toMatch(/Add Snapshot/);
//});
